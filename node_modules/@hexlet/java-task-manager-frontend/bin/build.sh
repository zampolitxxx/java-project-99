#! /usr/bin/env bash

CURRENT_PATH=$(pwd)
SCRIPT_DIR=${CURRENT_PATH}/node_modules/@hexlet/java-task-manager-frontend/bin

cd ${SCRIPT_DIR}/.. && npm run build

rm -rf ${CURRENT_PATH}/src/main/resources/static
mkdir -p ${CURRENT_PATH}/src/main/resources/static
cp -R ${SCRIPT_DIR}/../dist/* ${CURRENT_PATH}/src/main/resources/static
